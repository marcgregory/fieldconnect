import { ScheduleClient } from './ScheduleClient';

export const metadata = {
  title: 'Schedule - FieldConnect',
  description: 'Manage technician schedules',
};

export default function SchedulePage() {
  return <ScheduleClient />;
}
